{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$BRANCH\" = \"main\" ] || [ \"$BRANCH\" = \"master\" ]; then echo \"{\\\"decision\\\":\\\"block\\\",\\\"reason\\\":\\\"You are on the $BRANCH branch. Create a feature branch first (e.g. git checkout -b feat/your-task).\\\"}\"; fi",
            "statusMessage": "Checking branch..."
          }
        ]
      }
    ]
  }
}
